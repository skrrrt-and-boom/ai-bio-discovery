# Data Rules

- `raw/` contains downloaded source data and is treated as immutable.
- `processed/` contains reproducible transformations of raw data.
- Download URLs, versions, licenses, and checksums must be recorded before use.
- Large data files are ignored by Git.
- Private or identifiable human data is outside the scope of Experiment 001.

